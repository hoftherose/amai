services:
    api:
        build: .
        container_name: amai
        env_file: .env
        ports:
            - '8000:8000'
        restart: unless-stopped
        volumes:
            - .:/usr/src/app

