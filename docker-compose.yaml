services:
    amai:
        build: .
        container_name: shop
        env_file: .env
        ports:
            - '8000:8000'
        restart: unless-stopped
        volumes:
            - .:/usr/src/app

