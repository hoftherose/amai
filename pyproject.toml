[project]
name = "Amai"
version = "0.1.0"
description = "Backend for AI pipeline integration."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.115.12",
    "fastmcp>=2.2.0",
    "mcp[cli]>=1.6.0",
    "uvicorn>=0.34.1",
]
